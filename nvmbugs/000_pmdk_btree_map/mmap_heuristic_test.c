#include <stdio.h>
#include <stdlib.h>
#include <sys/mman.h>



int main(int argc, const char *argv[]) {

  size_t mmap_len = 16 * getpagesize();

  // void *stuff = mmap(NULL, 

  // return munmap(stuff, mmap_len);
}